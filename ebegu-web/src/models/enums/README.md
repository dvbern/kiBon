# DV kiBon Workspace

## Enum Naming

In this directory, there are several classes that start with the prefix TS.
This is actually an old naming pattern style that should not be used anymore. So for future reference,
just use the class name without any "TS" prefix.

Already existing enums can be migrated to the new naming style if wanted/needed.
