# Testdata

In this directory, you find scripts to create testdata for the mandants. Run one
of the scripts to create all required data such as users, institutionen, gemeinden
for the mandant.

Newer scripts rely on helper procedure. Make sure that these are created in your
database by running the _helper/testdata_procedures.sql_ script.

